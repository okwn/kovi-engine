{
  "name": "@kovi/api-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@kovi/db": "workspace:*",
    "@kovi/events": "workspace:*",
    "@kovi/source-sdk": "workspace:*",
    "fastify": "^5.2.0"
  }
}