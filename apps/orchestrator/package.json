{
  "name": "@kovi/orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "lint": "eslint src workflows activities --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@kovi/config": "workspace:*",
    "@kovi/db": "workspace:*",
    "@kovi/observability": "workspace:*",
    "@kovi/shared": "workspace:*",
    "@kovi/source-sdk": "workspace:*",
    "@temporalio/client": "^1.11.2",
    "@temporalio/worker": "^1.11.2",
    "@temporalio/workflow": "^1.11.2"
  },
  "devDependencies": {
    "tsx": "^4.19.3"
  }
}
